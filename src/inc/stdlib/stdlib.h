#ifndef USEFUL_H
#define USEFUL_H

/// @brief converte um inteiro 'n' de base 'base' para uma string = representação em texto do inteiro
/// @param n numero a ser convertido
/// @param base base do numero (ex: decimal(10), hex(16), binario(2)...)
char *itoa(int n, int base);

#endif //USEFUL_H